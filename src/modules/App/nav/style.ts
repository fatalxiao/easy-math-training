// Vendors
import {css} from '@emotion/react';

export default css`
    position: sticky;
    top: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px;
`;
