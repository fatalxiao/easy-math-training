// Vendors
import {css} from '@emotion/react';

export default css`
    display: flex;
    flex-direction: column;
    height: 100%;
    min-height: 100vh;

    .app-content {
        flex: 1;
    }
`;
